{
  "$schema": "http://iglucentral.com/schemas/com.snowplowanalytics.self-desc/schema/jsonschema/1-0-0#",
  "description": "Schema for a Google Analytics screenview hit. Additional properties are enabled so that indexed fields can be added without the need to change the schema.",
  "self": {
    "vendor": "com.google.analytics.measurement-protocol",
    "name": "screenview",
    "format": "jsonschema",
    "version": "1-0-0"
  },

  "type": "object",
  "properties": {
    "hitType": {
      "enum": [
        "screenview"
      ]
    },
    "nonInteractionHit": {
      "type": ["boolean", "null"]
    },
    "screenName": {
      "type": "string",
      "maxLength": 2048
    },
    "contentGroup1": {
      "type": ["string", "null"],
      "maxLength": 100
    }
  },
  "additionalProperties": true
}

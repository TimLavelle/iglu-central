{
  "$schema": "http://iglucentral.com/schemas/com.snowplowanalytics.self-desc/schema/jsonschema/1-0-0#",
  "description": "Schema for a Google Analytics screenview hit. Additional properties are enabled so that more content group fields can be added without the need to change the schema. GA allows up to 10 content groups per hit.",
  "self": {
    "vendor": "com.google.analytics.measurement-protocol",
    "name": "screenview",
    "format": "jsonschema",
    "version": "1-0-0"
  },

  "type": "object",
  "properties": {
    "hitType": {
      "enum": [
        "screenview"
      ]
    },
    "nonInteractionHit": {
      "type": "boolean"
    },
    "screenName": {
      "type": "string",
      "maxLength": 2048
    },
    "contentGroup1": {
      "type": "string",
      "maxLength": 100
    }
  },
  "additionalProperties": true
}
